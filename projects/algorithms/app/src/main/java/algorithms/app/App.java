/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package algorithms.app;

import algorithms.list.LinkedList;

import static algorithms.utilities.StringUtils.join;
import static algorithms.utilities.StringUtils.split;
import static algorithms.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
